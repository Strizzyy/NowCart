"""Models package — `domain` (internal) and `dto` (wire contracts).

Import from the subpackages directly, e.g.:
    from app.models.domain import Cart, Product
    from app.models.dto import CartResponse, OutcomeRequest
"""
